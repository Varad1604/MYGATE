# Deterministic installs across machines
save-exact=true
# Hoist only what is needed; NestJS/Prisma need some hoisting for binaries
shamefully-hoist=false
auto-install-peers=true
strict-peer-dependencies=false
